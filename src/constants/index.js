const ROLES = {
  USER: 'user',
  ADMIN: 'admin',
};

const PROMPT_STATUS = {
  PENDING: 'pending',
  APPROVED: 'approved',
};

const PAGINATION = {
  DEFAULT_PAGE: 1,
  DEFAULT_LIMIT: 20,
};

module.exports = {
  ROLES,
  PROMPT_STATUS,
  PAGINATION,
};
